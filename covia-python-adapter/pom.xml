<project xmlns="https://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>ai.covia</groupId>
		<artifactId>covia</artifactId>
		<version>0.9.5-SNAPSHOT</version>
	</parent>
	<artifactId>covia-python-adapter</artifactId>
	<name>Covia Python Adapter Module</name>
	<description>Optional loadable venue module for operator-configured in-process Python operations.</description>

	<properties>
		<!-- Skip ordinary deploy-plugin publication. The Central publishing
		     extension bundles reactor artifacts independently, including the
		     signed shaded module classifier used by venue operators. -->
		<maven.deploy.skip>true</maven.deploy.skip>
	</properties>

	<dependencies>
		<!-- Venue SPI supplied by the host and never bundled. -->
		<dependency>
			<groupId>ai.covia</groupId>
			<artifactId>venue</artifactId>
			<version>${project.version}</version>
			<scope>provided</scope>
		</dependency>
		<!-- The dependency-light runtime is bundled into the module jar. -->
		<dependency>
			<groupId>ai.covia</groupId>
			<artifactId>covia-python</artifactId>
			<version>${project.version}</version>
			<exclusions>
				<exclusion>
					<groupId>world.convex</groupId>
					<artifactId>convex-core</artifactId>
				</exclusion>
			</exclusions>
		</dependency>
		<dependency>
			<groupId>org.junit.jupiter</groupId>
			<artifactId>junit-jupiter-api</artifactId>
			<version>${junit.version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.junit.jupiter</groupId>
			<artifactId>junit-jupiter-engine</artifactId>
			<version>${junit.version}</version>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<version>3.5.6</version>
				<configuration>
					<argLine>--enable-native-access=ALL-UNNAMED</argLine>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-failsafe-plugin</artifactId>
				<version>3.5.6</version>
				<configuration>
					<argLine>--enable-native-access=ALL-UNNAMED</argLine>
					<systemPropertyVariables>
						<covia.module.jar>${project.build.directory}/${project.build.finalName}-module.jar</covia.module.jar>
						<covia.venue.jar>${project.parent.basedir}/venue/target/covia.jar</covia.venue.jar>
						<covia.test.classes>${project.build.testOutputDirectory}</covia.test.classes>
					</systemPropertyVariables>
				</configuration>
				<executions>
					<execution>
						<goals>
							<goal>integration-test</goal>
							<goal>verify</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-shade-plugin</artifactId>
				<version>3.6.2</version>
				<executions>
					<execution>
						<phase>package</phase>
						<goals><goal>shade</goal></goals>
						<configuration>
							<shadedArtifactAttached>true</shadedArtifactAttached>
							<shadedClassifierName>module</shadedClassifierName>
							<artifactSet>
								<excludes>
									<exclude>ai.covia:venue</exclude>
									<exclude>ai.covia:covia-core</exclude>
									<exclude>world.convex:*</exclude>
									<exclude>org.slf4j:*</exclude>
									<exclude>ch.qos.logback:*</exclude>
								</excludes>
							</artifactSet>
							<filters>
								<filter>
									<artifact>*:*</artifact>
									<excludes>
										<exclude>module-info.class</exclude>
										<exclude>META-INF/*.SF</exclude>
										<exclude>META-INF/*.DSA</exclude>
										<exclude>META-INF/*.RSA</exclude>
									</excludes>
								</filter>
							</filters>
							<transformers>
								<transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
							</transformers>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
</project>
