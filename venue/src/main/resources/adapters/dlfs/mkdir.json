{
  "name": "Create DLFS Directory",
  "description": "Create a directory in a DLFS drive. Set 'parents' to true to create missing intermediate directories (mkdir -p). Existing directories are accepted and reported with created=false.",
  "creator": "Covia",
  "operation": {
    "adapter": "dlfs:mkdir",
    "toolName": "dlfs_mkdir",
    "input": {
      "type": "object",
      "properties": {
        "drive": { "type": "string", "description": "Drive name" },
        "path": { "type": "string", "description": "Directory path to create" },
        "parents": { "type": "boolean", "description": "Create intermediate parents if missing (default false)" }
      },
      "required": ["drive", "path"]
    },
    "output": {
      "type": "object",
      "properties": {
        "created": { "type": "boolean", "description": "True if a new directory was created" },
        "path": { "type": "string", "description": "Drive-relative absolute path of the directory" }
      }
    }
  }
}
