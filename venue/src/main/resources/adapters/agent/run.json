{
	"name": "Run Agent",
	"description": "Runs the agent's transition loop: reads all messages from the inbox, invokes the specified transition operation once with the entire inbox, appends the result to the timeline, and clears the inbox. The agent transitions from sleeping to running and back to sleeping on completion.",
	"dateCreated": "2026-02-27T00:00:00Z",
	"operation": {
		"adapter": "agent:run",
		"input": {
			"type": "object",
			"properties": {
				"agentId": { "type": "string", "description": "Agent name to run" },
				"operation": { "type": "string", "description": "Transition operation reference (e.g. 'test:echo')" }
			},
			"required": ["agentId", "operation"]
		},
		"output": {
			"type": "object",
			"properties": {
				"agentId": { "type": "string" },
				"status": { "type": "string", "description": "Agent status after run (e.g. 'SLEEPING')" }
			}
		}
	}
}
