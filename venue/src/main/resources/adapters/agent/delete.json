{
	"name": "Delete Agent",
	"description": "Terminates and removes an agent. The agent will no longer accept messages, tasks, or triggers. The agent record is preserved for audit purposes.",
	"dateCreated": "2026-03-07T00:00:00Z",
	"operation": {
		"adapter": "agent:delete",
		"input": {
			"type": "object",
			"properties": {
				"agentId": { "type": "string", "description": "Agent name to delete" }
			},
			"required": ["agentId"]
		},
		"output": {
			"type": "object",
			"properties": {
				"agentId": { "type": "string" },
				"status": { "type": "string", "description": "Final status (TERMINATED)" }
			}
		}
	}
}
