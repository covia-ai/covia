{
	"name": "Suspend Agent",
	"description": "Manually suspends an agent, preventing any future runs until explicitly resumed. If the agent is currently running, the current run will complete but no further runs will start.",
	"dateCreated": "2026-03-07T00:00:00Z",
	"operation": {
		"adapter": "agent:suspend",
		"input": {
			"type": "object",
			"properties": {
				"agentId": { "type": "string", "description": "Agent name to suspend" }
			},
			"required": ["agentId"]
		},
		"output": {
			"type": "object",
			"properties": {
				"agentId": { "type": "string" },
				"status": { "type": "string", "description": "Agent status after suspension (SUSPENDED)" }
			}
		}
	}
}
