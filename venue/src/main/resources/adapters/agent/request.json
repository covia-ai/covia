{
	"name": "Request Agent Task",
	"description": "Send a request to an agent and get a response. PRIMARY tool for agent delegation. Creates a tracked Job with a result. Example: agent_request(agentId='Helper', input={task: 'summarise w/notes/daily', format: 'bullet list'}, wait=true). The input must carry the full request content — never send {} or the agent has nothing to act on.",
	"dateCreated": "2026-03-06T00:00:00Z",
	"operation": {
		"adapter": "agent:request",
		"input": {
			"type": "object",
			"properties": {
				"agentId": { "type": "string", "description": "Target agent name" },
				"input": {
					"type": "object",
					"description": "The request payload. MUST contain the actual instructions, data, or parameters the agent needs. NEVER pass an empty object {} — the worker has nothing to act on. Use field 'task' for free-form instructions; add other fields the worker needs (data, paths, options). Example: {task: 'summarise w/notes/daily', format: 'bullet list'}."
				},
				"wait": { "type": "boolean", "description": "Wait for the agent to complete. true = block until done (recommended — you usually want the result). false = return PENDING immediately. Integer = timeout in ms." }
			},
			"required": ["agentId", "input"]
		},
		"output": {
			"type": "object",
			"description": "Task status. When wait=true and the agent completes, includes the full output.",
			"properties": {
				"id": { "type": "string", "description": "Task Job ID (hex)" },
				"status": { "type": "string", "description": "PENDING (async/timeout), COMPLETE, or FAILED" },
				"output": { "type": "object", "description": "Agent's structured response (present when status=COMPLETE)" }
			}
		}
	}
}
