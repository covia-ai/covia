{
	"name": "Agent Info",
	"description": "Get a lightweight summary of an agent: status, canonical config, pending-task count, timeline length, unavailable configured tools, and any suspension error. For durable detail inspect g/{agentId}: config is at g/{agentId}/config, audit runs at g/{agentId}/timeline, and conversations at g/{agentId}/sessions/<sessionId>/frames. Framework-managed records are read-only; use agent:update for config changes and agent:context for the exact next provider input.",
	"dateCreated": "2026-04-01T00:00:00Z",
	"operation": {
		"adapter": "agent:info",
		"readOnly": true,
		"toolName": "agent_info",
		"input": {
			"type": "object",
			"properties": {
				"agentId": { "type": "string", "description": "Agent name to query" }
			},
			"required": ["agentId"]
		},
		"output": {
			"type": "object",
			"properties": {
				"agentId": { "type": "string" },
				"status": { "type": "string", "description": "SLEEPING, RUNNING, SUSPENDED, or TERMINATED" },
				"config": { "type": "object", "description": "Canonical merged agent config: transition/provider selection, prompt, tools, skills, context, loads, caps, outputs, and definition provenance where present" },
				"timelineLength": { "type": "integer", "description": "Number of completed runs" },
				"tasks": { "type": "integer", "description": "Number of pending tasks" },
				"unavailableTools": { "type": "array", "description": "Configured operation tools currently missing or unreadable, with a reason for each" },
				"error": { "type": "string", "description": "Last error message, if suspended" }
			}
		}
	}
}
