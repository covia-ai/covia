{
	"name": "Describe Function",
	"description": "Get the full metadata for an adapter function, including its input and output schemas. Use this to learn what parameters a function expects before calling it.",
	"dateCreated": "2026-03-11T00:00:00Z",
	"operation": {
		"adapter": "covia:describe",
		"toolName": "describe_function",
		"input": {
			"type": "object",
			"properties": {
				"name": { "type": "string", "description": "The function name, e.g. \"agent:create\", \"http:get\"" }
			},
			"required": ["name"]
		},
		"output": {
			"type": "object",
			"description": "The full asset metadata including operation schema"
		}
	}
}
