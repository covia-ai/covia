{
	"name": "Copy Lattice Value",
	"description": "Copy a value from any readable path to any writable path. Key use: pin a venue op as a named shortcut — covia_copy from=v/ops/json/merge to=o/merge, then grid_run operation=o/merge. Also useful for snapshotting data or branching workspace values. Both read and write capability checks apply.",
	"operation": {
		"adapter": "covia:copy",
		"input": {
			"type": "object",
			"properties": {
				"from": {
					"type": "string",
					"description": "Source path. Any resolvable address: hex hash, /a/<hash>, /o/<name>, /v/<path>, DID URL, or workspace path (w/, g/, j/, etc.)."
				},
				"to": {
					"type": "string",
					"description": "Destination path. Must be writable: w/, o/, n/, or t/."
				}
			},
			"required": ["from", "to"]
		},
		"output": {
			"type": "object",
			"properties": {
				"existed": {
					"type": "boolean",
					"description": "Whether a value was already present at the destination before the copy: false = created, true = an existing value was overwritten."
				},
				"pathCreated": {
					"type": "boolean",
					"description": "Present and true only if copying had to build a missing parent path at the destination (intermediate hierarchy). Omitted when copied into existing structure. Distinct from 'existed', which is about the destination leaf."
				}
			}
		}
	}
}
