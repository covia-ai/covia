{
	"name": "List Adapters",
	"description": "Lists all adapters registered on this venue with their name, description, and number of operations. Adapters are the execution backends that handle different types of operations (LLM inference, HTTP requests, agent lifecycle, lattice CRUD, etc.).",
	"dateCreated": "2026-03-25T00:00:00Z",
	"operation": {
		"adapter": "covia:adapters",
		"toolName": "covia_adapters",
		"input": {
			"type": "object",
			"properties": {}
		},
		"output": {
			"type": "object",
			"properties": {
				"adapters": {
					"type": "array",
					"description": "List of adapters with name, description, and operation count",
					"items": {
						"type": "object",
						"properties": {
							"name": { "type": "string" },
							"description": { "type": "string" },
							"operations": { "type": "integer" }
						}
					}
				}
			}
		}
	}
}
