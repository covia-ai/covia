{
  "name": "Get Asset",
  "description": "Retrieves an asset's metadata by its content-addressed ID. Returns the metadata as 'value', consistent with covia:read. Use asset:content to retrieve the content payload separately. Returns exists: false if the asset is not found.",
  "dateCreated": "2026-03-31T00:00:00Z",
  "operation": {
    "adapter": "asset:get",
    "toolName": "asset_get",
    "input": {
      "type": "object",
      "properties": {
        "id": { "type": "string", "description": "Asset ID (hex hash from asset:store)" }
      },
      "required": ["id"]
    },
    "output": {
      "type": "object",
      "properties": {
        "id": { "type": "string", "description": "Asset ID (confirmed)" },
        "exists": { "type": "boolean", "description": "True if the asset was found" },
        "value": { "type": "object", "description": "The stored metadata object (present when exists is true)" }
      }
    }
  }
}
