{
  "name": "List Directory",
  "description": "List entries in a directory under a configured filesystem root. Returns each entry's name, type (file/directory/symlink/other), size in bytes, and last-modified time (epoch millis). Omit 'path' to list the root itself.",
  "creator": "Covia",
  "operation": {
    "adapter": "file:list",
    "toolName": "file_list",
    "input": {
      "type": "object",
      "properties": {
        "root": { "type": "string", "description": "Configured root name (see file:roots)" },
        "path": { "type": "string", "description": "Directory path relative to the root (omit for root)" }
      },
      "required": ["root"]
    },
    "output": {
      "type": "object",
      "properties": {
        "entries": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "name": { "type": "string" },
              "type": { "type": "string" },
              "size": { "type": "integer" },
              "modified": { "type": "integer" }
            }
          }
        }
      }
    }
  }
}
