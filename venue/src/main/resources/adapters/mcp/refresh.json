{
	"name": "MCP Refresh Server",
	"description": "Reconciles a bridged MCP server's catalog against its live tools/list (#80): new tools are added, changed ones re-registered, vanished ones removed. Run after the remote server changes its tool set. Scope 'venue' requires the mcp/manage ability.",
	"dateCreated": "2026-07-16T00:00:00Z",
	"operation": {
		"adapter": "mcp:server:refresh",
		"input": {
			"type": "object",
			"properties": {
				"name": { "type": "string", "description": "Registry name of the server to refresh" },
				"scope": { "type": "string", "enum": ["user", "venue"], "description": "Default 'user'; 'venue' requires the mcp/manage ability" }
			},
			"required": ["name"]
		},
		"output": {
			"type": "object",
			"properties": {
				"name": { "type": "string" },
				"tools": { "type": "array" },
				"total": { "type": "integer" }
			}
		}
	}
}
