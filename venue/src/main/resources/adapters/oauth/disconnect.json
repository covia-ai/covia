{
  "name": "Disconnect an account",
  "description": "Forgets the caller's grant for a provider and, where the provider supports revocation, revokes it there too. Only when the user asks to disconnect.",
  "operation": {
    "adapter": "oauth:disconnect",
    "input": {
      "type": "object",
      "properties": {
        "provider": { "type": "string" }
      },
      "required": ["provider"]
    },
    "output": {
      "type": "object",
      "properties": {
        "provider": { "type": "string" },
        "disconnected": { "type": "boolean" },
        "revoked": { "type": "boolean", "description": "True when the provider confirmed revocation; false when it has no revocation endpoint or refused" }
      }
    }
  }
}
