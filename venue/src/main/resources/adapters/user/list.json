{
  "name": "List users",
  "description": "Lists users registered at this venue. Requires venue user-read authority.",
  "operation": {
    "adapter": "user:list",
    "readOnly": true,
    "input": {
      "type": "object",
      "properties": {}
    },
    "output": {
      "type": "object",
      "properties": {
        "users": { "type": "array" },
        "total": { "type": "integer" }
      },
      "required": ["users", "total"]
    }
  }
}
