{
  "name": "User sudo",
  "description": "Explicitly executes one operation in another user's namespace. Requires user/sudo on the target DID, plus the target operation's invoke and point-of-action capabilities. The authenticated caller remains the actor.",
  "operation": {
    "adapter": "user:sudo",
    "input": {
      "type": "object",
      "properties": {
        "did": {
          "type": "string",
          "description": "Target user DID whose namespace the nested operation executes in"
        },
        "operation": {
          "type": "string",
          "description": "Operation reference to execute"
        },
        "input": {
          "description": "Arguments for the nested operation"
        }
      },
      "required": ["did", "operation"]
    },
    "output": {
      "description": "The nested operation's output"
    }
  }
}
