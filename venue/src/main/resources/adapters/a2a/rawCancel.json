{
	"name": "Raw A2A Cancel Task",
	"description": "Diagnostic escape hatch: cancel a task directly by A2A URL with per-call Bearer authentication. Prefer importing an A2A agent asset.",
	"operation": {
		"adapter": "a2a:rawCancel",
		"secretFields": ["bearerSecret", "bearerToken"],
		"input": {
			"type": "object",
			"properties": {
				"url": { "type": "string", "description": "Base URL of the remote A2A agent" },
				"id": { "type": "string", "description": "Remote Task ID to cancel" },
				"bearerSecret": { "type": "string", "description": "Optional caller-owned SecretStore reference sent as HTTP Bearer auth." },
				"bearerToken": { "type": "string", "description": "Optional one-off literal Bearer token; redacted from Job records." }
			},
			"required": ["url", "id"]
		},
		"output": { "type": "object", "description": "The canceled A2A Task" }
	}
}
