{
	"name": "A2A Send Message",
	"description": "Send a message to a remote A2A agent. The local Covia Job mirrors the remote Task's lifecycle: status updates propagate, final artifacts become the Job's output. Pass `taskId` to continue an existing remote conversation.",
	"operation": {
		"adapter": "a2a:send",
		"internal": false,
		"secretFields": ["bearerSecret", "bearerToken"],
		"input": {
			"type": "object",
			"properties": {
				"url": {
					"type": "string",
					"description": "Base URL of the remote A2A agent"
				},
				"message": {
					"type": "object",
					"description": "A2A Message — {role, parts, messageId?}. Parts are stored as typed descriptors ({type:\"text\",text:...} or {type:\"data\",data:...})."
				},
				"taskId": {
					"type": "string",
					"description": "Optional remote Task ID. When set, sends a continuation to an existing remote Task; otherwise creates a new one."
				},
				"bearerSecret": {
					"type": "string",
					"description": "Optional caller-owned SecretStore reference (for example s/PARTNER_A2A_TOKEN) whose value is sent as a standard HTTP Bearer credential on the send and all subsequent task polls/cancellation. Mutually exclusive with bearerToken."
				},
				"bearerToken": {
					"type": "string",
					"description": "Optional literal bearer token for this send and all subsequent task polls/cancellation. Mutually exclusive with bearerSecret; redacted from Job records."
				}
			},
			"required": ["url", "message"]
		},
		"output": {
			"type": "object",
			"description": "The final remote A2A Task when it reaches a terminal state, or the current Task snapshot when it enters an interrupted state (INPUT_REQUIRED, AUTH_REQUIRED)."
		}
	}
}
