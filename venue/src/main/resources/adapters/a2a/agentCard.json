{
	"name": "A2A Agent Card",
	"description": "Read the immutable Agent Card snapshot from an imported A2A agent Asset",
	"operation": {
		"adapter": "a2a:getAgentCard",
		"readOnly": true,
		"input": {
			"type": "object",
			"properties": {
				"agent": {
					"type": "string",
					"description": "Imported A2A agent Asset reference, normally w/a2a/agents/<name> or its immutable did:.../a/<hash> ID."
				}
			},
			"required": ["agent"]
		},
		"output": {
			"type": "object",
			"description": "A2A Agent Card — see https://a2a-protocol.org/latest/specification/"
		}
	}
}
