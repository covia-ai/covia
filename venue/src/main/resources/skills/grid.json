{
	"name": "grid",
	"description": "Run operations on OTHER venues — cross-venue federation, remote jobs, hash-verified definitions. Load when work or data lives on a different venue.",
	"content": {
		"contentType": "text/markdown",
		"inline": "## Grid (cross-venue)\n- run `{operation, input, venue?}` — invoke on a remote venue and wait for the result.\n- invoke `{operation, input, venue?}` — async form; returns a job reference. Poll with job-status `{id, venue}` and fetch job-result `{id, venue, timeout?}`.\n- `venue` is a DID (`did:web:host`, `did:key:...`) or base URL. `operation` is the remote's catalog path (`v/ops/...`) or a `did:...:/a/<hash>` asset URL — remote definitions are fetched hash-verified, so any copy is authoritative.\n\nSemantics:\n- Execution happens where the operation lives; only inputs and outputs cross venues (data sovereignty).\n- A DOWN venue is an operational error (remote-fetch failure), distinct from \"no such operation\" — read the error text before concluding an op does not exist.\n- Long-running remote work: prefer invoke + status polling over a blocking run; remote jobs have no framework timeout.\n- Your identity rides the call; the remote venue applies its own capability rules to you."
	},
	"skill": {
		"tools": [
			"v/ops/grid/run",
			"v/ops/grid/invoke",
			"v/ops/grid/job-status",
			"v/ops/grid/job-result"
		]
	}
}
